app(app='myApp')
  router
    .home(scene='home', route='/', default='true')
      h1 home
    .about(scene='about', route='/about')
      h1 about
    .users(scene='users', route='/users')
      h1 users
script(src='dist/main.js')
